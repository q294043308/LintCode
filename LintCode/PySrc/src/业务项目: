业务项目:
    逻辑较为清晰
    偏向运营，个人想向技术方向发展
    web 的 http 调用端
    有用到对称加密
    不了解的情况下经常扯到其他的点

容器化：
    仅限于使用层面
    缺陷不了解

场景：
    高 qps 场景下设计 redis + db 的读场景
        预热
        设计存在一定的问题，引导后能明白，但无法进行有效修改

db：mvcc 机制 了解
redis: 类型+zset 跳转表 了解

kafka: 不了解

网络层：
    各个层的职能了解
    游戏场景用 udp / udp-tcp 之间关系了解
